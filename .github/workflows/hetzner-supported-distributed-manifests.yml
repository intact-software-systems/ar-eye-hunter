name: Run Hetzner Supported Distributed Manifests

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: hetzner-production-distributed-recipe
  cancel-in-progress: false
  queue: max

jobs:
  governance-decision:
    name: Classify authenticated governance decision
    runs-on: ubuntu-24.04
    permissions:
      actions: read
      contents: read
    outputs:
      decision_only: ${{ steps.resolve.outputs.decision_only }}
      invalid_governance: ${{ steps.resolve.outputs.invalid_governance }}
    steps:
      - name: Check out the exact main commit
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24

      - name: Detect a governance-shaped commit
        id: candidate
        continue-on-error: true
        run: |
          printf 'governance_candidate=true\n' >> "$GITHUB_OUTPUT"
          node scripts/governance-decisions/classify-governance-push.mjs candidate \
            --commit "$GITHUB_SHA" \
            --output "$GITHUB_OUTPUT"

      - name: Verify an exact decision-only commit
        id: verify
        if: ${{ github.event_name == 'push' && steps.candidate.outcome == 'success' && steps.candidate.outputs.governance_candidate == 'true' }}
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
          GOVERNANCE_APP_SLUG: ${{ vars.GOVERNANCE_APP_SLUG }}
        run: npm run governance:decide -- verify-commit --commit "$GITHUB_SHA"

      - name: Resolve fail-closed governance classification
        id: resolve
        if: ${{ always() }}
        env:
          EVENT_NAME: ${{ github.event_name }}
          CANDIDATE_OUTCOME: ${{ steps.candidate.outcome }}
          GOVERNANCE_CANDIDATE: ${{ steps.candidate.outputs.governance_candidate || 'true' }}
          VERIFICATION_OUTCOME: ${{ steps.verify.outcome }}
        run: >-
          node scripts/governance-decisions/classify-governance-push.mjs resolve
          --event-name "$EVENT_NAME"
          --candidate-outcome "$CANDIDATE_OUTCOME"
          --candidate "$GOVERNANCE_CANDIDATE"
          --verification-outcome "$VERIFICATION_OUTCOME"
          --output "$GITHUB_OUTPUT"

  invalid-governance-decision:
    name: Reject invalid governance-shaped commit
    needs: governance-decision
    if: needs.governance-decision.outputs.invalid_governance == 'true'
    runs-on: ubuntu-24.04
    steps:
      - name: Report invalid governance evidence
        run: |
          echo "::error::Governance-shaped commit failed authenticated decision verification"
          exit 1

  selection:
    name: Select distributed validation
    needs: governance-decision
    if: ${{ always() && !cancelled() }}
    runs-on: ubuntu-24.04
    outputs:
      selected: ${{ steps.selection.outputs.selected }}
      reason_code: ${{ steps.selection.outputs.reason_code }}
      reason: ${{ steps.selection.outputs.reason }}
    steps:
      - name: Check out the exact commit
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Classify distributed validation risk
        id: selection
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE: ${{ github.event.before || '' }}
          HEAD: ${{ github.sha }}
          DECISION_ONLY: ${{ needs.governance-decision.outputs.decision_only }}
        run: |
          if [ "$DECISION_ONLY" = 'true' ]; then
            printf 'selected=false\nreason_code=governance-decision\n' >> "$GITHUB_OUTPUT"
            printf 'reason=Distributed validation not selected: authenticated governance decision.\n' >> "$GITHUB_OUTPUT"
          else
            node scripts/distributed-validation-risk.mjs select \
              --repo-root "$GITHUB_WORKSPACE" \
              --event-name "$EVENT_NAME" \
              --base "$BASE" \
              --head "$HEAD" \
              --output "$GITHUB_OUTPUT"
          fi

      - name: Publish selection reason
        env:
          REASON: ${{ steps.selection.outputs.reason }}
          REASON_CODE: ${{ steps.selection.outputs.reason_code }}
        run: |
          printf '### Distributed validation selection\n\n%s\n\nReason code: `%s`\n' \
            "$REASON" "$REASON_CODE" >> "$GITHUB_STEP_SUMMARY"

  preflight:
    name: Validate shared preparation cohort
    needs: selection
    if: needs.selection.outputs.selected == 'true'
    runs-on: ubuntu-24.04
    steps:
      - name: Check out the exact commit
        uses: actions/checkout@v7

      - name: Reject topology-specific supported manifests
        run: |
          node scripts/github-actions/validate-hetzner-shared-preparation.mjs \
            apps/rallar-black-box/manifests/hetzner/01-health-2-agent.json \
            apps/rallar-black-box/manifests/hetzner/02-composite-evidence-2-agent.json \
            apps/rallar-black-box/manifests/hetzner/03-rtc-smoke-2-agent.json \
            apps/rallar-black-box/manifests/hetzner/04-provider-parity-2-agent.json \
            apps/rallar-black-box/manifests/hetzner/05a-rtc-realtime-stability-2-agent-5s.json

  prepare:
    name: Prepare Hetzner controller
    needs: [selection, preflight]
    if: needs.selection.outputs.selected == 'true' && needs.preflight.result == 'success'
    uses: ./.github/workflows/hetzner-distributed-recipe-runner.yml
    with:
      ref: ${{ github.sha }}
      operator_phase: prepare
      rollout_before_run: true
      run_id: main-${{ github.run_id }}-${{ github.run_attempt }}-prepare
      agent_prefix: controller
      manifest_path: apps/rallar-black-box/manifests/hetzner/01-health-2-agent.json
      register_before_login: true
      browser_log_level: warning
      headless_entry: headless
      browser_engine: chromium
      install_playwright: true
      npm_ci: false
      wait_for_agents: false
      ready_timeout_seconds: '120'
      terminal_timeout_seconds: '300'
      stop_after_run: false
    secrets: inherit

  run:
    name: ${{ matrix.manifest_id }}
    needs: [selection, prepare]
    if: needs.selection.outputs.selected == 'true' && needs.prepare.result == 'success'
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        include:
          - manifest_id: 01-health-2-agent
            manifest_path: apps/rallar-black-box/manifests/hetzner/01-health-2-agent.json
          - manifest_id: 02-composite-evidence-2-agent
            manifest_path: apps/rallar-black-box/manifests/hetzner/02-composite-evidence-2-agent.json
          - manifest_id: 03-rtc-smoke-2-agent
            manifest_path: apps/rallar-black-box/manifests/hetzner/03-rtc-smoke-2-agent.json
          - manifest_id: 04-provider-parity-2-agent
            manifest_path: apps/rallar-black-box/manifests/hetzner/04-provider-parity-2-agent.json
          - manifest_id: 05a-rtc-realtime-stability-2-agent-5s
            manifest_path: apps/rallar-black-box/manifests/hetzner/05a-rtc-realtime-stability-2-agent-5s.json
    uses: ./.github/workflows/hetzner-distributed-recipe-runner.yml
    with:
      ref: ${{ github.sha }}
      operator_phase: run
      rollout_before_run: false
      run_id: main-${{ github.run_id }}-${{ github.run_attempt }}-${{ matrix.manifest_id }}
      agent_prefix: controller
      manifest_path: ${{ matrix.manifest_path }}
      register_before_login: true
      browser_log_level: warning
      headless_entry: headless
      browser_engine: chromium
      install_playwright: false
      npm_ci: false
      wait_for_agents: true
      ready_timeout_seconds: '120'
      terminal_timeout_seconds: '300'
      stop_after_run: true
    secrets: inherit

  distributed-validation-result:
    name: Run Hetzner Supported Distributed Manifests result
    needs: [selection, preflight, prepare, run]
    if: always()
    runs-on: ubuntu-24.04
    steps:
      - name: Check out the exact commit
        uses: actions/checkout@v7

      - name: Conclude distributed validation
        env:
          SELECTED: ${{ needs.selection.outputs.selected }}
          SELECTION_RESULT: ${{ needs.selection.result }}
          PREFLIGHT_RESULT: ${{ needs.preflight.result }}
          PREPARE_RESULT: ${{ needs.prepare.result }}
          RUN_RESULT: ${{ needs.run.result }}
        run: |
          node scripts/distributed-validation-risk.mjs conclude \
            --selected "$SELECTED" \
            --selection-result "$SELECTION_RESULT" \
            --preflight-result "$PREFLIGHT_RESULT" \
            --prepare-result "$PREPARE_RESULT" \
            --run-result "$RUN_RESULT"
