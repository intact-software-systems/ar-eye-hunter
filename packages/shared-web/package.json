{
  "name": "@ar-eye-hunter/shared-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@js-temporal/polyfill": "^0.5.1"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "check:browser-bundles": "node scripts/measure-browser-bundles.mjs --check",
    "lint": "tsc -p tsconfig.json --noEmit",
    "measure:browser-bundles": "node scripts/measure-browser-bundles.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
