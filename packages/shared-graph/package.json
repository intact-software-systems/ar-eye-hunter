{
  "name": "@ar-eye-hunter/shared-graph",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "graphology": "0.26.0",
    "@js-temporal/polyfill": "^0.5.1"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --eval \"process.exit(0)\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
