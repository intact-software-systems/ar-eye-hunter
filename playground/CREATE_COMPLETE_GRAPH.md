# Create Complete Graph

Each SPA (node) joins one or more groups and establishes a WebRTC connection to all the members of the group to ping
them. This is probably good to have a constraint on.

The RTTs are sent to the server which stores the RTT information and uses Vivaldi as an algorithm to avoid huge
fluctuations in the RTT

which then gathers them and creates a complete graph.

The node gathers the .
